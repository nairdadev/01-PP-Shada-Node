[
{
    "uid": "sha",
    "append": true, // append the logs, do not overwrite
    "watch": false, // watch for changes and restart if they occur
    "script": "app.js",  // main startup script (almost like issuing node server.js)
    "sourceDir": "/var/www/html/shadasrl" // the dir where your entire app code resides (dir structure is recursively traversed)
}
]
